// Default export - Export add as default

// with naming
/* 
const add = (a, b) => {
    return a + b;
};
export default add;
*/

// with out naming
export default function(a, b) {
    return a + b;
}
