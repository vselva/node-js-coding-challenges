// instanceof is an operator in JavaScript 
// used to check if an object is an 
// instance of a specific class or constructor function.

class Person {}
const person1 = new Person();
console.log(person1 instanceof Person); // true